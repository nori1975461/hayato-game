// 「蒼神骸華」（そうしんがいか）＝マキナ四神柱の頂点・すべてのマキナの創造主。第5案（2026-09-15 22:05 FB＝炎・腕・武具・退廃を増やし、機械の要素を足す。退廃と機械を多めに）。
//
// 第4案への FB（22:05）：「かなり近づいてきた。炎の量・腕の本数・武具の種類・退廃の量を増やして。マキナは金属生命体なので機械の要素も。退廃と機械を多めに」
//   → 第5案＝第4案の上に重ねる（大枠と作法は据え置き）：
//     機械：円光を **歯車の輪**（歯は金・左は欠ける）／腕の上腕に **ピストン**・関節は **ボルト**／両肩の後ろに **排気管**（右は熱で揺らぐ・左は折れて煙）／
//           鎧の帯の下に **通気口**／左腹の **裂け目から歯車** と神経光／金棒の柄に **導管**（神経光）と頭に通気の溝／牙の座の下に **配管**
//     退廃：左肩の後ろから **配線が垂れて座まで届き火花** を散らす／裂けた腹／折れた排気管／**切れた鎖が座に落ちて** いる／座の左下の **油溜まり**／
//           冠の帯の亀裂・左の簾は2本とも切れる／右側にも錆の斑点と垂れ（清い側にも腐蝕が及び始める）
//     腕：六本 → **八本**（腰の後ろから第4の対。右＝三叉戟を外へ構える／左＝鎖鉤を握るが鎖は切れて座に落ちている）
//     武具：金棒・曲刀・三叉戟・鎖鉤（＋盃・日輪・月）
//     炎：舌の数と長さを増やし、根元が重なる炎の壁に。左も本数を増やす（死にかけのまま）
//   「目障りでない過剰」の作法（第4案から）：装飾は大きな形＋黒い輪郭・手は必ず何かを握る・後ろの腕は影に沈める・荘厳と退廃は左右で分ける
//
// 第3案への FB（21:40）：「大枠は気に入っている。仏様の特徴が強すぎる。阿修羅や酒吞童子のような悪魔的要素を全面に。武具。退廃も。
//   四神柱＝ごてごてした過剰な装飾がありながら目障りさはなく、荘厳と退廃が喧嘩せずに同居」→ 第4案＝仏の記号を鬼神の記号に差し替え
//   （火焔光背・骨の角・六本の腕と武具・牙の座・黒鉄の鎧）
// 第2案への FB（21:18）：「もっと退廃を・静謐が強すぎる」→ 第3案＝腐蝕の色は錆（暗い紫灰は黒い衣に沈んで等倍で見えない）
// 第1案への FB：「虫の要素が強い」→ 骨を細い部品で語ると節足動物へ寄る → 第2案＝仏像の文法。落とした稿＝一つ目／孔雀の羽／歯の口／青い頭巾／二つの目
//
// 名前の読み解き：蒼＝蒼白い骨鉄・蒼い硝子・蒼い炎／神＝もういない／骸＝形骸（形と装いだけが残る）／華＝蓮華化生（マキナは蓮から生まれる）
//   → 「創造主はとうに死んでいる。角の生えた冠だけが歯車の円光の中に浮かび、八本の骨の腕は武具と日月を掲げたまま止まり、
//      配線は垂れて火花を散らし、胸の蓮華の種子だけが今も次のマキナを孕んで光る」
//
// 構図（正面・世界座標は本体中心からのドット・scale 4.2）：
//   火焔光背（thruster）… 尖った帯の外に蒼い炎の壁。左の一区間が折れ、左の炎は暗く低い
//   後の腕（wingR/L）   … 高く掲げて右＝日輪・左＝欠けた月。影に沈む
//   中の腕（baseR/L）   … 横へ張って右＝曲刀・左＝傾いた盃から深紅
//   第4の腕（qlegFR/FL）… 腰の後ろから右＝三叉戟・左＝切れた鎖鉤（鎖は座に落ちる）
//   歯車の円光（podL）  … 開いた輪光の外縁に歯（金）・宝珠・左は欠けて曇る
//   排気管（trackR/L）  … 両肩の後ろ。右は熱で揺らぐ・左は折れて煙
//   牙の座（legL）      … 骨の牙・金の帯・黒鉄の段・その下に配管。左端は崩れ、油溜まり
//   配線（qlegBL）      … 左肩の後ろから垂れて座に届き、切れた先で火花
//   本体（body）        … 骨の襟 → 牙の首飾り → 牙つきの肩当て → 黒鉄の鎧（継ぎ目・金の縁・鋲）→ 金の帯 → 通気口 → 膝。左腹は裂けて歯車が覗く
//   角の冕冠（dome）    … 金の宝冠＋骨の角（左は折れる）＋玉の簾（左は2本とも切れる）。帯に亀裂
//   胸の蓮華（rack）／種子（core）
//   前の腕（armR/L）    … 右＝金棒（導管つき）を握る／左＝掌に枯れた蓮華（指2本欠け・上腕の板が剥がれて中の機構）
//
// 縛り：全パーツ新規・完了前に等倍で目視（[[feedback_pixel_art_judge_at_play_zoom]]）。⚠️ ゲーム未反映（役割は boss.js の深度表に合わせ済み）
import { g, P, GET, LN, ARC, AT, DISC, ELL, RECT, OUTLINE, R, dither, SPHERE, validate } from './god-raster.mjs';

export const PAL = {
  k: '#04060e',                                                           // 黒（輪郭・溝・空洞・油）
  j: '#1a2034', m: '#3b4661', f: '#7a89a6', s: '#bcc8db', n: '#f4f7ff',   // 骨鉄 4段＋稜線（蒼白い骨・黒鉄の影は藍）
  q: '#0a1f3c', Q: '#10457a', P: '#1d8ac9', N: '#62d8ff', C: '#d8f8ff',   // 蒼い硝子・蒼い炎 4段＋縁の光
  w: '#2a2338', u: '#5a4f6e',                                             // 枯れた花弁・死にかけの炎・煙
  c: '#38e1ff',                                                           // マキナの神経光（亀裂・導管・火花）
  y: '#7a5a12', Y: '#c9971f', G: '#ffd23f', W: '#ffedb0',                 // 金（冠・歯車・鎧の縁・帯・盃・日輪・鋲・環）
  r: '#8a1622', R: '#e03040', A: '#ff7a6a',                               // 深紅（種子・盃から零れる）
  d: '#2a1730',                                                           // 骨髄（折れ口）
  o: '#5a2e1e', O: '#9a5a30',                                             // 錆（亀裂から滲む・骨の斑点・枯れた蔓の茎）
};

const LIGHT = [-0.5, -0.62];   // 光は左上（他の三体と同じ）
const litSign = (nx, ny) => (LIGHT[0] * nx + LIGHT[1] * ny > 0 ? 1 : -1);
const shade4 = (v) => (v > 0.45 ? 's' : v > -0.05 ? 'f' : v > -0.5 ? 'm' : 'j');

// =====================================================================
// 花弁：根元 (bx,by) → 先端 (tx,ty)。蒼硝子＝地 Q・影側 q・光側の一筋 P・光側の縁 N・影側の縁 k・先端 C・中肋は骨。gilt＝縁と先端を金。withered＝枯れ
// =====================================================================
function petal(G, bx, by, tx, ty, hw, flare, opt = {}) {
  const dx = tx - bx, dy = ty - by, L = Math.hypot(dx, dy), ux = dx / L, uy = dy / L, nx = -uy, ny = ux;
  const ls = litSign(nx, ny), uEnd = opt.tornAt ?? 1;
  const at = (u) => [bx + ux * L * u + nx * flare * u * u, by + uy * L * u + ny * flare * u * u];
  const width = (u) => hw * Math.pow(Math.sin(Math.PI * (0.10 + 0.90 * u)), 0.55);
  for (let u = 0; u <= uEnd; u += 0.005) {
    const [cx, cy] = at(u), w = width(u);
    for (let k = -w; k <= w; k += 0.25) {
      const v = (k / w) * ls;
      let ch;
      if (opt.withered) ch = v > 0.6 ? 'u' : v > -0.8 ? 'w' : 'k';
      else if (u > 0.95) ch = opt.gilt ? 'W' : 'C';
      else if (v > 0.9) ch = opt.gilt ? 'Y' : 'N';
      else if (v > 0.55) ch = u < 0.25 ? 'Q' : 'P';
      else if (v > -0.45) ch = 'Q';
      else if (v > -0.9) ch = 'q';
      else ch = 'k';
      if (opt.gilt && v > 0.78 && v <= 0.9 && u > 0.3) ch = 'G';
      if (Math.abs(k) < 0.45 && u > 0.1 && u < 0.88) ch = opt.withered ? 'j' : (u < 0.35 ? 'm' : 'f');
      P(G, cx + nx * k, cy + ny * k, ch);
    }
  }
}
// 炎の舌：根元 (bx,by) → 先端 (tx,ty)。根元が太く先へ細る。flare で先端が曲がる。dying＝死にかけ（暗く・青白い芯だけ）
function flame(G, bx, by, tx, ty, hw, flare, dying = false) {
  const dx = tx - bx, dy = ty - by, L = Math.hypot(dx, dy), ux = dx / L, uy = dy / L, nx = -uy, ny = ux;
  const at = (u) => [bx + ux * L * u + nx * flare * u * u, by + uy * L * u + ny * flare * u * u];
  for (let u = 0; u <= 1; u += 0.004) {
    const [cx, cy] = at(u), w = Math.max(0.35, hw * Math.pow(1 - u, 0.55));
    for (let k = -w; k <= w; k += 0.25) {
      const a = Math.abs(k) / w;
      let ch;
      if (dying) ch = a < 0.3 && u > 0.3 ? 'P' : a < 0.7 ? 'Q' : 'w';
      else ch = u > 0.88 ? 'C' : a < 0.3 && u > 0.15 ? 'N' : a < 0.68 ? 'P' : 'Q';
      P(G, cx + nx * k, cy + ny * k, ch);
    }
  }
}
// 太い骨（腕の節・角・牙）：(x0,y0)→(x1,y1)、半幅 w0→w1。左上が光
function bone(G, x0, y0, x1, y1, w0, w1) {
  const dx = x1 - x0, dy = y1 - y0, L = Math.hypot(dx, dy), ux = dx / L, uy = dy / L, nx = -uy, ny = ux, ls = litSign(nx, ny);
  for (let u = 0; u <= 1; u += 0.004) {
    const w = w0 + (w1 - w0) * u, cx = x0 + ux * L * u, cy = y0 + uy * L * u;
    for (let k = -w; k <= w; k += 0.25) {
      const v = (k / w) * ls;
      P(G, cx + nx * k, cy + ny * k, v > 0.6 ? 's' : v > -0.25 ? 'f' : v > -0.75 ? 'm' : 'j');
    }
  }
}
// ピストン（機械）：骨の影側に沿う筒と、中から出る光る棒
function piston(G, x0, y0, x1, y1, w) {
  const dx = x1 - x0, dy = y1 - y0, L = Math.hypot(dx, dy), ux = dx / L, uy = dy / L, nx = -uy, ny = ux, ls = litSign(nx, ny), off = -ls * (w * 0.55);
  for (let u = 0.22; u <= 0.82; u += 0.004) {
    const cx = x0 + ux * L * u + nx * off, cy = y0 + uy * L * u + ny * off;
    for (let k = -1.1; k <= 1.1; k += 0.25) P(G, cx + nx * k, cy + ny * k, Math.abs(k) > 0.7 ? 'k' : u < 0.52 ? 'j' : 'n');   // 手前半分は筒（黒鉄）・奥半分は棒（光る）
  }
}
// 握り拳（正面・何かを握っている）：中心 (px,py)。骨の楕円に指の溝3本
function fist(G, px, py, rx = 3.3, ry = 3.0) {
  for (let y = -ry; y <= ry; y += 0.25) for (let x = -rx; x <= rx; x += 0.25) {
    if ((x / rx) ** 2 + (y / ry) ** 2 > 1) continue;
    P(G, px + x, py + y, shade4(-(x / rx * LIGHT[0] + y / ry * LIGHT[1]) * 0.9 - 0.1));
  }
  for (const dy of [-1.2, 0.3, 1.8]) for (let x = -rx + 1; x <= rx - 0.5; x += 0.25) P(G, px + x, py + dy, 'k');
}
// 関節つきの腕：点列 [[x,y],...] を太い骨で結ぶ。関節はボルト（黒い輪・十字）。上腕にピストン
function arm(G, pts, w0 = 3.4, w1 = 2.5) {
  for (let i = 0; i + 1 < pts.length; i++) {
    const wa = w0 + (w1 - w0) * (i / (pts.length - 1)), wb = w0 + (w1 - w0) * ((i + 1) / (pts.length - 1));
    bone(G, pts[i][0], pts[i][1], pts[i + 1][0], pts[i + 1][1], wa, wb);
    if (i === 0) piston(G, pts[0][0], pts[0][1], pts[1][0], pts[1][1], wa);
    if (i + 1 < pts.length - 1) {
      const [jx, jy] = pts[i + 1];
      DISC(G, jx, jy, wb + 0.3, 'k'); DISC(G, jx, jy, wb - 0.4, 'f'); P(G, jx - 1, jy - 1, 's');
      P(G, jx, jy, 'k'); P(G, jx - 1, jy, 'k'); P(G, jx + 1, jy, 'k'); P(G, jx, jy - 1, 'k'); P(G, jx, jy + 1, 'k');   // ボルトの十字
    }
  }
}
const dimBone = (G) => { for (const row of G) for (let x = 0; x < row.length; x++) row[x] = { s: 'f', f: 'm', m: 'j', n: 'f' }[row[x]] || row[x]; };   // 後ろの腕は影に沈める
const rustSpeck = (G, x0, x1, y0, y1, mod) => { for (let y = y0; y <= y1; y++) for (let x = x0; x <= x1; x++) { const ch = GET(G, x, y); if ((ch === 's' || ch === 'f') && (x * 5 + y * 3) % mod === 0) P(G, x, y, 'o'); } };
// 鎖：点列に沿って環（金）を並べる
function chain(G, pts, pitch = 2.2) {
  for (let i = 0; i + 1 < pts.length; i++) {
    const [x0, y0] = pts[i], [x1, y1] = pts[i + 1], L = Math.hypot(x1 - x0, y1 - y0), n = Math.max(1, Math.round(L / pitch));
    for (let k = 0; k <= n; k++) { const u = k / n, x = x0 + (x1 - x0) * u, y = y0 + (y1 - y0) * u; DISC(G, x, y, 1.3, 'k'); DISC(G, x, y, 0.9, k % 2 ? 'Y' : 'y'); P(G, x, y, 'k'); }
  }
}

// =====================================================================
// ① 火焔光背（thruster）100×82。row 0＝世界 -46・帯の頂は row 5（-41）・底 +35。帯（厚さ 4.2・蒼硝子・内縁は金）の外に蒼い炎の壁。
//    右の炎は生きて高く多い。左の炎は死にかけ（低く暗い）。左の一区間（世界 -20〜-13）は折れて無い
// =====================================================================
const MAN_W = 100, MAN_H = 82, MAN_TOP = 5;
const MANDORLA = (() => {
  const G = g(MAN_W, MAN_H), cx = 49.5, TH = 4.2, BR0 = 26, BR1 = 33;
  const hwAt = (y) => { const t = (y - MAN_TOP) / (MAN_H - 1 - MAN_TOP); if (t < 0) return 0; return 36 * Math.pow(Math.max(0, Math.sin(Math.PI * Math.pow(t, 0.8))), 0.6); };
  for (const [wy, len, side] of [[-38, 8, 1], [-34, 14, 1], [-30, 10, 1], [-26, 16, 1], [-22, 11, 1], [-18, 15, 1], [-14, 10, 1], [-10, 14, 1], [-6, 9, 1], [-2, 13, 1], [2, 9, 1], [6, 12, 1], [10, 8, 1], [14, 10, 1], [18, 7, 1], [22, 8, 1], [26, 5, 1],
    [-38, 5, -1], [-33, 8, -1], [-27, 6, -1], [-21, 9, -1], [-15, 6, -1], [-9, 8, -1], [-3, 6, -1], [3, 7, -1], [9, 5, -1], [15, 6, -1], [21, 4, -1]]) {   // 炎の舌（帯より先に描く）
    const y = wy + 46, hw = hwAt(y), bx = cx + side * (hw - 1.5), dx = side * 0.55, dy = -1, n = Math.hypot(dx, dy);
    flame(G, bx, y, bx + dx / n * len, y + dy / n * len, len * 0.5, -side * 3.0, side < 0);
  }
  flame(G, cx, 6.5, cx, 0.5, 2.8, 0.5, false); flame(G, cx - 3, 8, cx - 4.5, 2.5, 1.6, 0.8, false); flame(G, cx + 3, 8, cx + 4.5, 2.5, 1.6, -0.8, false);   // 頂の炎
  for (let y = MAN_TOP; y < MAN_H; y++) {
    const hw = hwAt(y);
    for (let x = 0; x < MAN_W; x++) {
      const d = hw - Math.abs(x - cx);
      if (d < 0 || d > TH) continue;
      const left = x < cx;
      if (left && y >= BR0 && y <= BR1) continue;
      let ch;
      if (left) ch = d < 1.2 ? 'P' : d < 3.0 ? 'Q' : 'G';
      else ch = d < 1.4 ? 'q' : d < 3.0 ? (dither(x, y) && d < 2.0 ? 'q' : 'Q') : 'Y';
      if (y < MAN_TOP + 7 && d >= 3.0) ch = 'W';
      if (y > 53 && left && d < 1.2) ch = 'Q';
      if (left && y >= 15 && y <= 51 && d < 2.0 && dither(x, y) && (y * 7 + x) % 3 === 0) ch = 'w';
      P(G, x, y, ch);
    }
  }
  for (const y of [BR0 - 1, BR1 + 1]) { const hw = hwAt(y); for (let d = 0; d <= TH; d += 0.25) P(G, cx - hw + d, y, d > 1.2 && d < 3.0 ? 'd' : 'k'); }
  P(G, cx - hwAt(BR0 - 1) - 1, BR0 - 2, 'C'); P(G, cx - hwAt(BR1 + 1) + 2, BR1 + 2.5, 'C');
  for (const y of [38, 46, 58]) { const hw = hwAt(y); LN(G, cx - hw + 0.5, y - 1, cx - hw + TH - 0.5, y + 1, 'k'); P(G, cx - hw + 2, y, 'c'); }
  for (const [wx, wy, ch] of [[-30, -6, 'u'], [-26, -1, 'w'], [-29, -13, 'u'], [-24, 4, 'w'], [-28, 8, 'u'], [-22, -19, 'u'], [-27, -24, 'w'],
    [24, 10, 'N'], [29, -1, 'C'], [22, 12, 'N'], [30, -12, 'N'], [31, -3, 'C']]) P(G, cx + wx, wy + 46, ch);
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ② 歯車の円光（podL）41×41。中心 (20,20)＝世界 (0,-22)・外径 17・内径 9 の開いた輪光。外縁に **歯**（金・16枚・左の4枚は欠けて根元だけ）と宝珠。
//    左下（150°〜190°）が欠け、欠けの周りは硝子が曇る。帯を横切る亀裂
// =====================================================================
const HALO_W = 41, HALO_H = 41;
const HALO = (() => {
  const G = g(HALO_W, HALO_H), c = 20;
  for (let i = 0; i < 16; i++) {                                                                       // 歯
    const a = i * 22.5, t = a * Math.PI / 180, broken = a >= 135 && a <= 202.5;
    for (let r = 16.6; r <= (broken ? 17.8 : 19.4); r += 0.25) for (let da = -4.5; da <= 4.5; da += 0.5) {
      const tt = (a + da) * Math.PI / 180, x = c + Math.cos(tt) * r, y = c + Math.sin(tt) * r;
      P(G, x, y, broken ? (r > 17.3 ? 'k' : 'y') : (da < -2 ? 'G' : da > 2.5 ? 'y' : 'Y'));
    }
    if (broken) P(G, c + Math.cos(t) * 17.6, c + Math.sin(t) * 17.6, 'd');
  }
  for (let y = 0; y < HALO_H; y++) for (let x = 0; x < HALO_W; x++) {
    const dx = x - c, dy = y - c, r = Math.hypot(dx, dy);
    if (r > 17.2 || r < 9.0) continue;
    const ang = (Math.atan2(dy, dx) * 180 / Math.PI + 360) % 360;
    if (ang >= 150 && ang <= 190 && r > 11.2) continue;
    const lit = dx * LIGHT[0] + dy * LIGHT[1];
    let ch;
    if (r > 15.6) ch = lit > 6 ? 'G' : lit < -6 ? 'y' : 'Y';
    else if (r > 14.7) ch = 'k';
    else if (r < 9.9) ch = lit > 3 ? 'G' : 'Y';
    else {
      const v = dy / 14.7;
      ch = v < -0.6 ? (dither(x, y) ? 'N' : 'P') : v < -0.2 ? (dither(x, y) ? 'P' : 'Q') : v < 0.25 ? 'Q' : v < 0.65 ? (dither(x, y) ? 'q' : 'Q') : 'q';
      if (((ang > 112 && ang < 150) || (ang > 190 && ang < 238)) && dither(x, y)) ch = 'w';
    }
    P(G, x, y, ch);
  }
  for (const a of [150, 190]) for (let r = 11.2; r <= 17.2; r += 0.25) AT(G, c, c, r, r, a, r > 12 && r < 16 ? 'd' : 'k');
  P(G, c - 13.5, c + 9, 'C');
  for (const a of [236, 258, 282, 304]) {                                                             // 宝珠（歯と歯の間）
    const t = a * Math.PI / 180, sx = c + Math.cos(t) * 16.3, sy = c + Math.sin(t) * 16.3;
    DISC(G, sx, sy, 1.4, 'G'); P(G, sx - 0.5, sy - 0.5, 'W'); P(G, sx + 0.7, sy + 0.7, 'Y');
  }
  for (const a of [214]) { const t = a * Math.PI / 180, sx = c + Math.cos(t) * 16.3, sy = c + Math.sin(t) * 16.3; DISC(G, sx, sy, 1.3, 'k'); P(G, sx, sy, 'd'); }   // 抜けた宝珠
  for (const a of [225, 300]) {
    const t = a * Math.PI / 180;
    LN(G, c + Math.cos(t) * 10, c + Math.sin(t) * 10, c + Math.cos(t) * 15.4, c + Math.sin(t) * 15.4, 'k');
    if (a === 225) P(G, c + Math.cos(t) * 12.5, c + Math.sin(t) * 12.5, 'c');
  }
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ③ 牙の座（legL）78×14。上向きの骨の牙7本・金の帯・黒鉄の段・その下に **配管**（黒鉄の筒・継ぎ目は金）。
//    左端は崩れて瓦礫・左の牙は折れ／錆びる・落ちた光背の欠片・散った枯れ花弁・左下に **油溜まり**
// =====================================================================
const PED_W = 78, PED_H = 14;
const PEDESTAL = (() => {
  const G = g(PED_W, PED_H), cx = 38.5;
  for (let x = 1; x <= 76; x++) for (let y = 8; y <= 10; y++) P(G, x, y, (Math.floor((x + 3) / 7) % 2 ? 'j' : 'm'));   // 黒鉄の段
  for (let x = 4; x <= 76; x += 7) for (let y = 8; y <= 10; y++) P(G, x, y, 'k');
  for (let x = 3; x <= 74; x++) { P(G, x, 11, 'f'); P(G, x, 12, 'm'); P(G, x, 13, 'j'); }                 // 配管（筒）
  for (let x = 3; x <= 74; x += 12) for (let y = 11; y <= 13; y++) { P(G, x, y, 'k'); P(G, x + 1, y, y === 12 ? 'Y' : 'y'); P(G, x + 2, y, 'k'); }   // 継ぎ目の環
  for (let x = 2; x <= 75; x++) { const u = (x - 2) / 73; P(G, x, 6, u < 0.3 ? 'G' : u < 0.62 ? 'Y' : 'y'); P(G, x, 7, u < 0.3 ? 'Y' : 'y'); }   // 金の帯
  for (let i = 0; i < 7; i++) {                                                                        // 牙
    const px = cx + (i - 3) * 11.5, lean = Math.sign(i - 3) * Math.min(1, Math.abs(i - 3) * 0.5);
    if (i === 0) { bone(G, px, 6.5, px + lean * 0.8, 4.2, 2.4, 1.9); for (let k = -1.9; k <= 1.9; k += 0.25) P(G, px + lean * 0.8 + k, 4, Math.abs(k) < 1 ? 'd' : 'k'); continue; }
    bone(G, px, 6.5, px + lean * 1.5, 2.8, 2.4, 1.6);
    bone(G, px + lean * 1.5, 2.8, px + lean * 3.2, 0.4, 1.6, 0.6);
    P(G, px + lean * 3.2, 0.3, 'n');
    P(G, px + lean * 0.7 - 1, 4.5, 'k'); P(G, px + lean * 0.7 + 1, 4.5, 'k');
  }
  rustSpeck(G, 5, 30, 1, 7, 4); rustSpeck(G, 44, 76, 1, 7, 9);
  for (const [x, y, ch] of [[15, 6, 'u'], [24, 7, 'w'], [33, 6, 'u'], [19, 7, 'u'], [45, 7, 'w'], [58, 6, 'u']]) P(G, x, y, ch);
  for (let x = 2; x <= 14; x++) for (let y = 6; y <= 7; y++) if (dither(x, y)) P(G, x, y, 'y');
  for (let y = 0; y <= 10; y++) for (let x = 0; x <= 6 - Math.floor(y / 4); x++) P(G, x, y, '.');    // 左端は崩れて無く、瓦礫が転がる
  for (const [x, y, ch] of [[1, 9, 'm'], [2, 9, 'f'], [3, 10, 'm'], [0, 10, 'j'], [4, 10, 'j'], [2, 10, 'm'], [5, 9, 'w'], [3, 8, 'u'], [1, 10, 'f'], [6, 10, 'o'], [4, 9, 'o']]) P(G, x, y, ch);
  for (let x = 0; x <= 11; x++) P(G, x, 13, 'k'); for (let x = 2; x <= 9; x++) P(G, x, 12, 'k'); P(G, 4, 12, 'j'); P(G, 7, 13, 'j');   // 油溜まり（配管の継ぎ目から漏れる）
  P(G, 9, 3, 'N'); P(G, 8, 4, 'k'); P(G, 9, 4, 'P'); P(G, 10, 4, 'Q'); P(G, 11, 4, 'k'); P(G, 7, 5, 'k'); P(G, 8, 5, 'P'); P(G, 9, 5, 'Q'); P(G, 10, 5, 'q'); P(G, 11, 5, 'k');
  for (const [x, y] of [[20, 11], [21, 12], [21, 13], [50, 11], [50, 12]]) P(G, x, y, 'O');           // 配管の錆
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ④ 本体（body）46×40。上端＝襟（世界 -16）・下端＝膝（+23）。黒鉄の鎧（板の継ぎ目・金の縁・鋲）・帯の下に通気口・左腹の裂け目から歯車と神経光
// =====================================================================
const TORSO_W = 46, TORSO_H = 40;
const TORSO = (() => {
  const G = g(TORSO_W, TORSO_H), cx = 22.5;
  const hwAt = (y) => (y <= 8 ? 8.5 + 10.5 * Math.sqrt(Math.max(0, (y - 1) / 7)) : y <= 22 ? 19 - 2 * (y - 8) / 14 : y <= 34 ? 17 + 6 * (y - 22) / 12 : 23);
  for (let y = 1; y < TORSO_H; y++) {
    const hw = hwAt(y);
    for (let x = Math.ceil(cx - hw); x <= Math.floor(cx + hw); x++) {
      const u = (x - cx) / hw;
      let ch = u < -0.55 ? 'm' : 'j';
      if (y === 13 || y === 21) ch = 'k';
      if (y === 14 || y === 22) ch = u < 0 ? 'Y' : 'y';
      if (y === 25) ch = u < -0.3 ? 'G' : u < 0.3 ? 'Y' : 'y';
      if (y === 26) ch = u < -0.3 ? 'Y' : 'y';
      P(G, x, y, ch);
    }
  }
  for (let y = 9; y <= 24; y++) if (y !== 13 && y !== 21) P(G, cx, y, 'k');
  for (const y of [15, 23]) for (const x of [cx - 15, cx - 10, cx + 10, cx + 15]) { P(G, x, y, 'Y'); P(G, x, y - 0.5, 'W'); }
  for (let y = 27; y <= 29; y++) for (let x = cx - 6; x <= cx + 6; x++) P(G, x, y, y === 28 ? 'k' : 'm');   // 通気口（帯の下）
  for (const x of [cx - 3, cx + 3]) for (let y = 27; y <= 29; y++) P(G, x, y, 'k');
  for (const [kx, lit] of [[cx - 12, true], [cx + 12, false]]) {
    for (let y = 29; y < TORSO_H; y++) for (let x = Math.ceil(kx - 10.5); x <= Math.floor(kx + 10.5); x++) {
      const ddx = (x - kx) / 10.5, ddy = (y - 35) / 5.2;
      if (ddx * ddx + ddy * ddy > 1) continue;
      P(G, x, y, shade4(-(ddx * LIGHT[0] + ddy * LIGHT[1]) * 0.7 - (lit ? 0.2 : 0.55)));
    }
  }
  for (let y = 31; y < TORSO_H; y++) P(G, cx, y, 'k');
  for (let y = 35; y < TORSO_H; y++) for (let x = cx - 21; x <= cx - 15 + (y - 35) * 0.4; x++) P(G, x, y, y === 35 || x <= cx - 20 ? 'k' : 'd');
  P(G, cx - 17, 37, 'c');
  for (const [dir, lit] of [[-1, true], [1, false]]) {
    for (let layer = 0; layer < 2; layer++) {
      const y0 = 4 + layer * 3, x0 = cx + dir * (10 + layer * 2), x1 = cx + dir * (17 + layer * 2);
      for (let y = y0; y < y0 + 3; y++) for (let x = Math.min(x0, x1); x <= Math.max(x0, x1); x++) {
        let ch = y === y0 ? 'k' : y === y0 + 1 ? (lit ? 's' : 'f') : (lit ? 'f' : 'm');
        if (dir * (x - cx) > 16 + layer * 2) ch = y === y0 ? 'k' : 'm';
        P(G, x, y, ch);
      }
      P(G, x1 - dir, y0 + 1, 'n');
    }
    if (dir > 0) { bone(G, cx + 18, 5, cx + 21, 1.5, 1.6, 0.8); P(G, cx + 21.5, 1, 'n'); }
    else { bone(G, cx - 18, 5, cx - 19.5, 3.2, 1.6, 1.3); P(G, cx - 19.5, 3, 'd'); P(G, cx - 20.5, 3, 'k'); P(G, cx - 18.5, 2.5, 'k'); }
  }
  for (let x = cx + 12; x <= cx + 20; x++) P(G, x, 10, 'Y');
  for (let x = cx - 19; x <= cx - 15; x++) P(G, x, 10, 'y');
  LN(G, cx - 16, 4, cx - 15, 9, 'k'); P(G, cx - 15, 6, 'c');
  for (let d = 0; d <= 1; d += 0.25) ARC(G, cx, 2, 12 + d, 6.5 + d, 25, 128, d < 0.5 ? 'G' : 'Y');
  for (const a of [45, 68, 90, 112]) { const t = a * Math.PI / 180, tx = cx + Math.cos(t) * 12.6, ty = 2 + Math.sin(t) * 7.4; bone(G, tx, ty, tx, ty + 2.6, 0.9, 0.3); P(G, tx, ty + 2.8, 'n'); }
  LN(G, cx - 7.5, 7.5, cx - 9, 12, 'Y'); P(G, cx - 9, 13, 'G'); P(G, cx - 9, 14, 'W');
  for (let y = -3; y <= 3; y += 0.25) for (let x = -9; x <= 9; x += 0.25) {
    const dd = (x / 9) ** 2 + (y / 3) ** 2; if (dd > 1) continue;
    P(G, cx + x, 2 + y, shade4(-(x / 9 * LIGHT[0] + y / 3 * LIGHT[1]) * 0.9 - 0.1));
  }
  ELL(G, cx, 1.2, 5.5, 1.7, 'k');
  for (let x = cx - 4; x <= cx + 4; x++) P(G, x, 0, 'k');
  // 退廃（左）＋機械
  LN(G, cx - 15, 9, cx - 12, 16, 'o'); LN(G, cx - 12, 16, cx - 14, 22, 'o'); LN(G, cx - 14, 22, cx - 10, 28, 'o');
  LN(G, cx - 14, 9, cx - 11, 16, 'k'); LN(G, cx - 11, 16, cx - 13, 22, 'k'); LN(G, cx - 13, 22, cx - 9, 28, 'k'); P(G, cx - 11, 16, 'c'); P(G, cx - 13, 22, 'c'); P(G, cx - 12, 12, 'c'); P(G, cx - 10, 26, 'c');
  LN(G, cx - 3, 4, cx - 2, 8, 'k'); P(G, cx - 3, 6, 'c'); P(G, cx - 4, 5, 'o'); P(G, cx - 3, 8, 'o');
  LN(G, cx + 9, 28, cx + 11, 34, 'k'); P(G, cx + 10, 30, 'o');
  LN(G, cx + 14, 11, cx + 17, 19, 'k'); P(G, cx + 15, 14, 'o'); P(G, cx + 16, 17, 'o');                // 右の鎧にも亀裂と錆（腐蝕が及び始める）
  for (let x = cx - 17; x <= cx - 10; x++) { P(G, x, 4, 'k'); P(G, x, 5, x > cx - 16 && x < cx - 11 ? 'd' : 'k'); P(G, x, 6, 'm'); }
  for (let y = 16; y <= 21; y++) for (let x = cx - 17 + Math.abs(y - 18.5) * 0.8; x <= cx - 9 - Math.abs(y - 18.5) * 0.5; x++) P(G, x, y, 'k');   // 左腹の裂け目
  DISC(G, cx - 13, 18.5, 2.3, 'f'); DISC(G, cx - 13, 18.5, 1.3, 'k'); for (const a of [0, 60, 120, 180, 240, 300]) AT(G, cx - 13, 18.5, 2.9, 2.9, a, 'f');   // 中の歯車
  P(G, cx - 10, 17, 'c'); P(G, cx - 16, 20, 'c'); P(G, cx - 11, 21, 'd');
  for (const [x, y, ch] of [[cx + 10, 27, 'O'], [cx + 10, 28, 'o'], [cx + 14, 27, 'O'], [cx + 14, 28, 'o'], [cx + 14, 29, 'o'], [cx + 3, 27, 'o'], [cx - 4, 27, 'O'], [cx - 4, 28, 'o'], [cx - 4, 29, 'o'], [cx + 17, 23, 'O'], [cx + 17, 24, 'o']]) P(G, x, y, ch);
  for (let y = 27; y < TORSO_H; y++) for (let x = cx - 23; x <= cx - 9; x++) { const ch = GET(G, x, y); if (ch && ch !== '.' && ch !== 'k' && (x * 3 + y * 5) % 7 === 0) P(G, x, y, (x + y) % 2 ? 'o' : 'w'); }
  for (let y = 30; y < TORSO_H; y++) for (let x = cx + 9; x <= cx + 20; x++) { const ch = GET(G, x, y); if (ch && ch !== '.' && ch !== 'k' && (x * 3 + y * 5) % 9 === 0) P(G, x, y, 'o'); }
  const vine = (pts) => { for (let i = 0; i + 1 < pts.length; i++) LN(G, pts[i][0], pts[i][1], pts[i + 1][0], pts[i + 1][1], 'o'); for (const [x, y] of pts) P(G, x, y, 'O'); };
  const blossom = (x, y) => { P(G, x, y, 'u'); P(G, x - 1, y, 'w'); P(G, x + 1, y, 'w'); P(G, x, y - 1, 'w'); P(G, x, y + 1, 'w'); };
  vine([[cx - 22, 39], [cx - 20, 34], [cx - 16, 31], [cx - 12, 28], [cx - 10, 30]]);
  vine([[cx - 15, 39], [cx - 14, 36], [cx - 11, 34]]);
  vine([[cx - 21, 30], [cx - 19, 24], [cx - 18, 18], [cx - 16, 12], [cx - 14, 8]]);
  blossom(cx - 20, 33); blossom(cx - 12, 28); blossom(cx - 11, 35); blossom(cx - 19, 24); blossom(cx - 16, 12);
  for (const [x, y] of [[cx - 15, 25], [cx - 16, 25], [cx - 9, 26], [cx - 17, 26], [cx - 20, 25], [cx - 21, 26], [cx + 18, 25]]) P(G, x, y, 'j');
  rustSpeck(G, cx - 8, cx - 2, 0, 4, 6); rustSpeck(G, cx - 19, cx - 12, 7, 9, 5); rustSpeck(G, cx + 12, cx + 19, 5, 9, 7);
  for (const x of [cx - 12, cx - 6, cx, cx + 6, cx + 12]) P(G, x, 25, 'W');
  P(G, cx + 12, 13, 'N'); P(G, cx + 14, 10, 'C');
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑤ 角の冕冠（dome）40×26。row 0＝世界 -40。金の宝冠（帯 row 13..15）＋骨の角（右は反る・左は折れて骨髄）＋玉の簾（左は2本とも切れる）。帯に亀裂
// =====================================================================
const CROWN_W = 40, CROWN_H = 26;
const CROWN = (() => {
  const G = g(CROWN_W, CROWN_H), cx = 19.5, B = 13;
  bone(G, 29.5, B + 0.5, 34, B - 5, 2.3, 1.8); bone(G, 34, B - 5, 37, B - 10, 1.8, 1.2); bone(G, 37, B - 10, 38.2, B - 12.6, 1.2, 0.6); P(G, 38.4, B - 13, 'n');
  P(G, 33, B - 3.5, 'k'); P(G, 35.4, B - 4.6, 'k'); P(G, 36, B - 8, 'k'); P(G, 37.8, B - 8.6, 'k');
  for (const [x, y] of [[32, B - 2], [35, B - 6.5]]) { P(G, x, y, 'Y'); P(G, x + 1, y - 1, 'Y'); }                                          // 角の金の環
  bone(G, 9.5, B + 0.5, 5.5, B - 4, 2.3, 1.9); for (let k = -1.9; k <= 1.9; k += 0.25) P(G, 5.5 + k * 0.7, B - 4.2 - k * 0.7, Math.abs(k) < 1 ? 'd' : 'k');
  P(G, 4.2, B - 5.6, 'c'); P(G, 7, B - 2, 'o');
  for (let x = 10; x <= 29; x++) { P(G, x, B, x < cx ? 'W' : 'G'); P(G, x, B + 1, x < cx ? 'G' : 'Y'); P(G, x, B + 2, x < cx ? 'Y' : 'y'); }
  for (const x of [13, 26]) P(G, x, B + 1, 'k');
  P(G, 16, B + 1, 'q'); P(G, 23, B + 1, 'q'); P(G, 19, B + 1, 'q'); P(G, 20, B + 1, 'Q');
  LN(G, 14, B, 15, B + 2, 'k'); P(G, 15, B + 1, 'o');                                                                                        // 帯の亀裂
  for (let y = B - 9; y <= B - 1; y++) { const hw = y < B - 6 ? 0.5 : 1.5; for (let x = Math.ceil(cx - hw); x <= Math.floor(cx + hw); x++) P(G, x, y, x < cx ? (y < B - 6 ? 'W' : 'G') : 'Y'); }
  P(G, 19, B - 9, 'W'); P(G, 20, B - 9, 'W');
  for (const [c0, lit] of [[14.5, true], [25.5, false]]) {
    for (let y = B - 5; y <= B - 1; y++) { const hw = 0.5 + (y - (B - 5)) * 0.5; for (let x = Math.ceil(c0 - hw); x <= Math.floor(c0 + hw); x++) P(G, x, y, x < c0 ? (lit ? 'W' : 'G') : (lit ? 'G' : 'Y')); }
  }
  for (let y = B - 5; y <= B - 4; y++) for (let x = 13; x <= 16; x++) P(G, x, y, '.');
  P(G, 13, B - 3, 'k'); P(G, 14, B - 3, 'd'); P(G, 15, B - 3, 'd'); P(G, 16, B - 3, 'k');
  for (let y = B; y <= B + 2; y++) for (let x = 10; x <= 14; x++) if (dither(x, y)) P(G, x, y, y === B + 2 ? 'o' : 'y');
  for (const [x, end] of [[11, B + 4], [13, B + 5], [26, B + 7], [28, B + 7]]) {                      // 玉の簾（左は2本とも切れる）
    for (let y = B + 3; y <= end; y++) P(G, x, y, y === end ? 'q' : y % 2 ? 'N' : 'Q');
    if (end < B + 7) P(G, x, end + 1, 'k');
  }
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑥ 胸の蓮華（rack）17×17／⑦ 種子（core）9×9
// =====================================================================
const LOTUS = (() => {
  const W = 17, H = 17, G = g(W, H), c = 8;
  DISC(G, c, c, 8.3, 'k'); DISC(G, c, c, 7.5, 'Y'); ARC(G, c, c, 7.5, 7.5, 190, 280, 'G'); ARC(G, c, c, 6.9, 6.9, 200, 260, 'W'); DISC(G, c, c, 6.6, 'q');
  for (let a = -90; a < 270; a += 45) {
    const t = a * Math.PI / 180;
    petal(G, c + Math.cos(t) * 1.8, c + Math.sin(t) * 1.8, c + Math.cos(t) * 6.4, c + Math.sin(t) * 6.4, 2.0, 0, { gilt: true, withered: a === 135 || a === 180 });
  }
  for (let r = 6.4; r <= 8.3; r += 0.25) for (let a = 160; a <= 200; a += 2) AT(G, c, c, r, r, a, 'k');
  LN(G, 0, 8, 2, 7, 'f'); P(G, 0, 9, 'f'); P(G, 1, 8, 's');
  for (const a of [0, 45, 90, 225, 270, 315]) { const t = a * Math.PI / 180; P(G, c + Math.cos(t) * 7.9, c + Math.sin(t) * 7.9, 'k'); }   // 輪の鋲（機械の縁）
  return R(G);
})();
const SEED = (() => {
  const W = 9, H = 9, G = g(W, H), c = 4;
  SPHERE(G, c, c, 4.3, ['y', 'Y', 'G', 'W', 'n'], [-0.5, -0.62, 0.6], 0.1);
  for (let y = -2; y <= 2; y += 0.25) { const w = 1.2 * Math.sqrt(Math.max(0, 1 - (y / 2.1) ** 2)) * (0.55 + 0.45 * (y + 2.1) / 4.2); for (let x = -w; x <= w; x += 0.25) P(G, c + x, c + y + 0.3, x < -w * 0.5 ? 'R' : 'r'); }
  P(G, c - 1, c - 1, 'A');
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑧ 前の腕（armR/armL・深度11）
//   右 24×64：肩 (3,24)＝世界 (17,-9)。肘 (12,38) → 手首 (12,30) → 拳 (12,27) が金棒（黒鉄・金の鋲と環・柄に神経光の導管・頭に通気の溝）を握る
//   左 26×44：肩 (22,4)＝世界 (-17,-9)。肘 (16,19) → 手首 (23,27) → 掌 (24,31) を上に向け枯れた蓮華を載せる（指2本欠け）。上腕の板が剥がれ中の機構
// =====================================================================
const ARMR_W = 24, ARMR_H = 64;
const ARM_R = (() => {
  const G = g(ARMR_W, ARMR_H), sx = 12;
  for (let y = 0; y <= 18; y++) { const hw = y < 2 || y > 16 ? 2 : 3; for (let x = sx - hw; x <= sx + hw; x++) P(G, x, y, x === sx - hw ? 'f' : x < sx ? 'm' : x === sx + hw ? 'k' : 'j'); }
  for (const y of [1, 17]) for (let x = sx - 3; x <= sx + 3; x++) P(G, x, y, x < sx ? 'G' : 'Y');
  for (const y of [4, 9, 14]) for (const x of [sx - 2, sx + 1]) { P(G, x, y, 'Y'); P(G, x + 1, y, 'Y'); P(G, x, y + 1, 'Y'); P(G, x + 1, y + 1, 'y'); P(G, x, y, 'W'); }
  for (const y of [6, 7, 11, 12]) P(G, sx, y, 'k');                                                    // 頭の通気の溝
  for (let y = 19; y <= 59; y++) for (let x = sx - 1; x <= sx + 1; x++) P(G, x, y, x === sx - 1 ? 'f' : x === sx ? 'm' : 'j');
  for (let y = 20; y <= 56; y += 2) P(G, sx + 1, y, 'c');                                              // 柄の導管（神経光）
  for (const y of [22, 36, 50]) for (let x = sx - 1; x <= sx + 1; x++) P(G, x, y, 'Y');
  for (let y = 57; y <= 60; y++) for (let x = sx - 2; x <= sx + 2; x++) P(G, x, y, y === 57 ? 'G' : 'Y');
  arm(G, [[3, 24], [12, 38], [12, 30]], 3.4, 2.6);
  fist(G, 12, 27, 3.4, 3.2);
  rustSpeck(G, 2, 10, 24, 40, 9);
  OUTLINE(G);
  return R(G);
})();
const ARML_W = 26, ARML_H = 44;
const ARM_L = (() => {
  const G = g(ARML_W, ARML_H);
  arm(G, [[22, 4], [16, 19], [23, 27]], 3.4, 2.5);
  for (let y = 8; y <= 12; y++) for (let x = 17 + (y - 8) * 0.3; x <= 21 - (y - 8) * 0.4; x++) P(G, x, y, 'k');   // 上腕の板が剥がれて中の機構
  P(G, 19, 10, 'c'); P(G, 18, 11, 'd'); P(G, 20, 9, 'j'); P(G, 19, 12, 'o');
  for (let y = -4.2; y <= 4.2; y += 0.25) for (let x = -3.8; x <= 3.8; x += 0.25) {
    const dd = (x / 3.8) ** 2 + (y / 4.2) ** 2; if (dd > 1) continue;
    P(G, 24 + x, 31 + y, shade4(-(x / 3.8 * LIGHT[0] + y / 4.2 * LIGHT[1]) * 0.8 - 0.1));
  }
  for (let i = 0; i < 4; i++) {
    const fx = 24 - 3.5 + i * 1.75 + 0.875;
    if (i >= 2) { for (let k = -0.8; k <= 0.8; k += 0.25) { P(G, fx + k, 27.2, 'k'); P(G, fx + k, 27.7, Math.abs(k) < 0.4 ? 'd' : 'k'); } continue; }
    for (let t = 0; t <= 3; t += 0.25) for (let k = -0.8; k <= 0.8; k += 0.25) P(G, fx + k, 27.4 - t, k < -0.3 ? 's' : 'f');
    P(G, fx, 24.2, 'n');
    if (i > 0) for (let t = 0; t <= 3; t += 0.25) P(G, fx - 0.95, 27.4 - t, 'k');
  }
  for (let d = 0; d <= 0.5; d += 0.25) ARC(G, 24, 31, 3.9 + d, 4.3 + d, 200, 340, d < 0.25 ? 'Y' : 'y');
  petal(G, 24, 29.5, 24, 24.5, 1.6, 0, { withered: true }); petal(G, 23, 29.5, 20.5, 25.5, 1.4, 0, { withered: true }); petal(G, 25, 29.5, 27.5, 25.5, 1.4, 0, { withered: true });
  P(G, 24, 29, 'd'); P(G, 28, 33, 'u');
  LN(G, 18.3, 24.9, 22.3, 21.9, 'k'); P(G, 20, 23, 'd'); P(G, 21, 23, 'd'); P(G, 19, 24, 'c'); P(G, 20, 24, 'O'); P(G, 19, 25, 'o');
  for (const [x, y, ch] of [[21, 27, 'w'], [22, 28, 'u'], [26, 27, 'w'], [26, 28, 'u'], [27, 29, 'w']]) P(G, x, y, ch);
  rustSpeck(G, 14, 25, 4, 18, 6);
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑨ 中の腕（baseR/baseL・深度7）右＝曲刀を外上へ掲げる／左＝傾いた盃から深紅が垂れる
// =====================================================================
const BASER_W = 35, BASER_H = 41;
const BASE_R = (() => {
  const G = g(BASER_W, BASER_H);
  arm(G, [[2, 30], [18, 34], [21, 24]], 3.2, 2.4);
  const bx0 = 22, by0 = 18.5, bx1 = 33, by1 = 1, dx = bx1 - bx0, dy = by1 - by0, L = Math.hypot(dx, dy), ux = dx / L, uy = dy / L, nx = -uy, ny = ux;
  for (let u = 0; u <= 1; u += 0.004) {
    const w = 2.6 * (1 - u * 0.7), cx = bx0 + ux * L * u + nx * 1.8 * u * u, cy = by0 + uy * L * u + ny * 1.8 * u * u;
    for (let k = -w; k <= w; k += 0.25) P(G, cx + nx * k, cy + ny * k, k > w * 0.35 ? (u > 0.93 ? 'n' : 's') : k > -w * 0.3 ? 'f' : k > -w * 0.75 ? 'm' : 'j');
  }
  for (let u = 0.15; u <= 0.85; u += 0.01) { const cx = bx0 + ux * L * u + nx * 1.8 * u * u, cy = by0 + uy * L * u + ny * 1.8 * u * u; P(G, cx - nx * 0.9, cy - ny * 0.9, 'c'); }   // 刃の導管（連続した線＝点の群れにしない）
  for (let k = -3.5; k <= 3.5; k += 0.25) { P(G, bx0 + nx * k, by0 + ny * k + 0.5, 'Y'); P(G, bx0 + nx * k, by0 + ny * k + 1.5, 'y'); }
  fist(G, 22, 21.5, 3.2, 3.0);
  rustSpeck(G, 2, 20, 26, 38, 9);
  OUTLINE(G);
  return R(G);
})();
const BASEL_W = 29, BASEL_H = 27;
const BASE_L = (() => {
  const G = g(BASEL_W, BASEL_H);
  arm(G, [[26, 17], [11, 21], [12, 11]], 3.2, 2.4);
  for (let y = -2.6; y <= 2.6; y += 0.25) for (let x = -3.4; x <= 3.4; x += 0.25) {
    const rx = x * 0.94 + y * 0.34, ry = -x * 0.34 + y * 0.94;
    if ((rx / 3.4) ** 2 + (ry / 2.6) ** 2 > 1 || ry < -1.2) continue;
    P(G, 9.5 + x, 4.5 + y, rx < -1 ? 'W' : rx < 1.2 ? 'G' : 'Y');
  }
  for (let x = 6.5; x <= 12.5; x += 0.25) P(G, x, 2.3 + (x - 6.5) * -0.36 + 1.6, 'k');
  for (let x = 7; x <= 11.5; x += 0.25) P(G, x, 3.2 + (x - 7) * -0.36 + 1.2, x < 8.5 ? 'R' : 'r');
  for (let y = 4; y <= 21; y += 0.25) P(G, 6.2 - (y - 4) * 0.04, y, y < 6 ? 'A' : y % 3 < 1.5 ? 'R' : 'r');
  P(G, 5.4, 22.5, 'R'); P(G, 6.4, 24, 'r'); P(G, 5.8, 26, 'R');
  fist(G, 13, 8, 3.2, 3.0);
  rustSpeck(G, 8, 27, 8, 22, 5);
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑩ 後の腕（wingR/wingL・深度7＝円光の後ろ）右＝日輪／左＝欠けた月。影に沈める
// =====================================================================
const WINGR_W = 23, WINGR_H = 34;
const WING_R = (() => {
  const G = g(WINGR_W, WINGR_H);
  arm(G, [[2, 31], [21, 19], [16, 9]], 3.2, 2.4); fist(G, 15, 7.5, 3.0, 2.6); dimBone(G);
  DISC(G, 15, 3.6, 3.6, 'k'); SPHERE(G, 15, 3.6, 3.1, ['y', 'Y', 'G', 'G', 'W'], [-0.5, -0.62, 0.6], 0.12);
  OUTLINE(G);
  return R(G);
})();
const WINGL_W = 24, WINGL_H = 35;
const WING_L = (() => {
  const G = g(WINGL_W, WINGL_H);
  arm(G, [[21, 32], [2, 20], [7, 10]], 3.2, 2.4); fist(G, 8, 7.5, 3.0, 2.6); rustSpeck(G, 2, 22, 10, 33, 5); dimBone(G);
  DISC(G, 8, 4, 3.6, 'k'); DISC(G, 8, 4, 3.0, 'N'); DISC(G, 8, 4, 2.2, 'P');
  DISC(G, 9.6, 3.2, 2.7, '.'); ARC(G, 9.6, 3.2, 2.7, 2.7, 120, 250, 'k');
  for (let a = 200; a <= 240; a += 3) AT(G, 8, 4, 3.0, 3.0, a, 'k'); P(G, 5.6, 2.8, 'd');
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑪ 第4の腕（qlegFR/qlegFL・深度7＝腰の後ろから出る）
//   右 40×36：肩 (2,26)＝世界 (12,6)。肘 (20,32)＝(30,12) → 手首 (25,22) → 拳 (26,20)＝(36,0) が **三叉戟** を外上へ構える
//   左 31×43：肩 (28,24)＝世界 (-12,6)。肘 (10,30)＝(-30,12) → 手首 (7,22) → 拳 (6,20)＝(-34,2) が **鎖鉤** を握るが、鎖は切れて座に落ちている
// =====================================================================
const QR_W = 40, QR_H = 36;
const QLEG_R = (() => {
  const G = g(QR_W, QR_H);
  arm(G, [[2, 26], [20, 32], [25, 22]], 3.2, 2.4);
  bone(G, 26, 23, 33.5, 7, 1.4, 1.1);                                                                // 三叉戟の柄
  for (let u = 0; u <= 1; u += 0.05) { const x = 26 + 7.5 * u, y = 23 - 16 * u; if (u % 0.2 < 0.05) P(G, x + 0.8, y + 0.4, 'Y'); }
  for (let k = -3.2; k <= 3.2; k += 0.25) P(G, 33.5 + k * 0.9, 7 + k * 0.42, 'Y');                     // 横木（金）
  bone(G, 33.5, 7, 35.8, 1.2, 1.0, 0.4); P(G, 36, 0.8, 'n');                                           // 中の刃
  bone(G, 30.8, 8.3, 31.5, 2.6, 0.9, 0.4); P(G, 31.5, 2.2, 'n');
  bone(G, 36.2, 5.8, 38.6, 1.6, 0.9, 0.4); P(G, 38.8, 1.2, 'n');
  fist(G, 26, 20.5, 3.0, 2.8);
  rustSpeck(G, 2, 22, 20, 34, 9);
  OUTLINE(G);
  return R(G);
})();
const QL_W = 31, QL_H = 43;
const QLEG_L = (() => {
  const G = g(QL_W, QL_H);
  arm(G, [[28, 24], [10, 30], [7, 22]], 3.2, 2.4);
  fist(G, 6, 20.5, 3.0, 2.8);
  chain(G, [[5.5, 24], [7, 33]]);                                                                       // 拳から垂れる鎖
  DISC(G, 7.6, 35.5, 1.3, 'k'); P(G, 7.6, 35.5, 'd');                                                  // 切れた環
  chain(G, [[10, 41], [19, 41]]);                                                                       // 座に落ちた鎖
  bone(G, 20, 41, 24, 38.5, 1.2, 0.5); bone(G, 24, 38.5, 22.5, 36.5, 0.7, 0.3); P(G, 22.3, 36.2, 'n');   // 鉤
  rustSpeck(G, 8, 30, 20, 32, 5); P(G, 12, 41, 'o'); P(G, 16, 42, 'o'); P(G, 21, 40, 'O');
  OUTLINE(G);
  return R(G);
})();

// =====================================================================
// ⑫ 排気管（trackR/trackL・深度7＝円光より後・本体より前）11×27：両肩の後ろから2本。右は熱で揺らぐ（N の粒）・左は1本が折れて煙（w/u）
// =====================================================================
const STK_W = 11, STK_H = 27;
const stack = (broken) => {
  const G = g(STK_W, STK_H);
  for (const [x0, top] of [[2, broken ? 12 : 5], [6, broken ? 4 : 8]]) {
    for (let y = top; y < STK_H; y++) for (let x = x0; x <= x0 + 2; x++) P(G, x, y, x === x0 ? 'f' : x === x0 + 1 ? 'm' : 'j');
    for (const y of [top + 3, top + 9, top + 15]) for (let x = x0; x <= x0 + 2; x++) P(G, x, y, x === x0 ? 'G' : 'Y');
    for (let x = x0; x <= x0 + 2; x++) P(G, x, top, 'k');
    if (broken && x0 === 2) { P(G, x0, top - 1, 'k'); P(G, x0 + 1, top - 1, 'd'); P(G, x0 + 2, top - 1, 'k'); P(G, x0 - 1, top + 2, 'o'); P(G, x0 + 3, top + 4, 'o'); }
  }
  if (broken) for (const [x, y, ch] of [[3, 8, 'w'], [2, 6, 'u'], [4, 5, 'w'], [1, 3, 'u'], [3, 2, 'w'], [7, 1, 'u'], [8, 0, 'w']]) P(G, x, y, ch);   // 煙
  else for (const [x, y, ch] of [[3, 3, 'N'], [2, 1, 'C'], [7, 6, 'N'], [8, 4, 'C'], [6, 2, 'N']]) P(G, x, y, ch);                                   // 熱の揺らぎ
  OUTLINE(G);
  return R(G);
};
const STACK_R = stack(false), STACK_L = stack(true);

// =====================================================================
// ⑬ 配線（qlegBL・深度7）16×40：左肩の後ろ（世界 -19,-14）から垂れて座（+24）に届く。3本。1本は切れて先が火花・1本は座の継ぎ口（金）に刺さる
// =====================================================================
const CAB_W = 16, CAB_H = 40;
const CABLES = (() => {
  const G = g(CAB_W, CAB_H);
  const cable = (pts, ch) => { for (let i = 0; i + 1 < pts.length; i++) { const [x0, y0] = pts[i], [x1, y1] = pts[i + 1]; for (let u = 0; u <= 1; u += 0.02) { const x = x0 + (x1 - x0) * u, y = y0 + (y1 - y0) * u; P(G, x - 0.9, y, 'k'); P(G, x, y, ch); P(G, x + 0.9, y, 'k'); } } };
  cable([[12, 0], [8, 8], [5, 18], [4, 28], [5, 38]], 'm');
  cable([[13, 1], [11, 10], [9, 22], [10, 30]], 'j');
  cable([[14, 2], [13, 9], [12, 14]], 'm');
  DISC(G, 12.5, 15, 1.4, 'k'); P(G, 12.5, 15, 'c'); P(G, 11.5, 16.5, 'c'); P(G, 13.8, 16.2, 'c'); P(G, 12.2, 17.8, 'c');   // 切れた先の火花
  P(G, 10, 31, 'c');
  for (let x = 3; x <= 7; x++) { P(G, x, 38, 'Y'); P(G, x, 39, 'y'); }                                // 座の継ぎ口
  P(G, 4, 24, 'o'); P(G, 9, 26, 'o'); P(G, 5, 33, 'O');
  OUTLINE(G);
  return R(G);
})();

export const GAIKA = {
  id: 'gaika',
  name: '蒼神骸華',
  concept: '牙の座に結跏趺坐する創造主の亡骸＝金属生命体。頭は無く、歯車の円光の中に骨の角が生えた金の冕冠だけが浮かぶ（左の角は折れる）。'
    + '八本の骨の腕（上腕にピストン・関節はボルト）：前の右手は導管つきの金棒を握って座に突き立て、前の左手は掌に枯れた蓮華（指2本欠け・上腕の板が剥がれる）。'
    + '中の右手は曲刀を掲げ、中の左手は傾いた盃から深紅を零す。後の両手は円光の後ろから日輪と欠けた月。第4の右手は三叉戟を構え、第4の左手の鎖鉤は鎖が切れて座に落ちている。'
    + '両肩の後ろに排気管（右は熱で揺らぎ、左は折れて煙）。左肩の後ろから配線が垂れて座に届き、切れた先で火花。背に蒼い炎の壁（右は生きて高く、左は死にかけ）。'
    + '黒鉄の鎧に金の縁と鋲、帯の下に通気口、左腹の裂け目から歯車と神経光。胸に金の輪の蓮華（芯の種子＝唯一の生きている光）。牙の座の下に配管、左下に油溜まり。'
    + '退廃は左に厚く、右にも錆が及び始めている。',
  sprites: {
    mandorla: { rows: MANDORLA, palette: PAL },
    halo: { rows: HALO, palette: PAL },
    pedestal: { rows: PEDESTAL, palette: PAL },
    torso: { rows: TORSO, palette: PAL },
    crown: { rows: CROWN, palette: PAL },
    lotus: { rows: LOTUS, palette: PAL },
    armR: { rows: ARM_R, palette: PAL },
    armL: { rows: ARM_L, palette: PAL },
    baseR: { rows: BASE_R, palette: PAL },
    baseL: { rows: BASE_L, palette: PAL },
    wingR: { rows: WING_R, palette: PAL },
    wingL: { rows: WING_L, palette: PAL },
    qlegR: { rows: QLEG_R, palette: PAL },
    qlegL: { rows: QLEG_L, palette: PAL },
    stackR: { rows: STACK_R, palette: PAL },
    stackL: { rows: STACK_L, palette: PAL },
    cables: { rows: CABLES, palette: PAL },
    seed: { rows: SEED, palette: PAL },
  },
  // 深度は boss.js / render-boss-rig の PART_DEPTH：thruster 6 < wing/base/qleg/track/pod/leg 7（並び順で後が上）< body 8 < dome/rack 9 < arm 11 < core 12
  rig: [
    { role: 'thruster', tex: 'mandorla', ox: 0, oy: -46, origin: [0.5, 0] },                   // 炎の頂 -46・帯の頂 -41・底 +35
    { role: 'wingR', tex: 'wingR', ox: 10, oy: -16, origin: [2 / WINGR_W, 31 / WINGR_H] },      // 後の腕
    { role: 'wingL', tex: 'wingL', ox: -10, oy: -16, origin: [21 / WINGL_W, 32 / WINGL_H] },
    { role: 'baseR', tex: 'baseR', ox: 14, oy: -12, origin: [2 / BASER_W, 30 / BASER_H] },      // 中の腕
    { role: 'baseL', tex: 'baseL', ox: -14, oy: -12, origin: [26 / BASEL_W, 17 / BASEL_H] },
    { role: 'qlegFR', tex: 'qlegR', ox: 12, oy: 6, origin: [2 / QR_W, 26 / QR_H] },             // 第4の腕（右）
    { role: 'podL', tex: 'halo', ox: 0, oy: -22, origin: [0.5, 0.5] },                          // 歯車の円光 -42.5..-1.5
    { role: 'trackR', tex: 'stackR', ox: 13, oy: -26, origin: [0, 0] },                         // 排気管（世界 x 13..23・y -26..0）
    { role: 'trackL', tex: 'stackL', ox: -13, oy: -26, origin: [1, 0] },
    { role: 'legL', tex: 'pedestal', ox: 0, oy: 22, origin: [0.5, 0] },                        // 牙の座 +22..+36
    { role: 'qlegBL', tex: 'cables', ox: -19, oy: -14, origin: [12 / CAB_W, 0] },              // 配線（左肩の後ろ → 座）
    { role: 'qlegFL', tex: 'qlegL', ox: -12, oy: 6, origin: [28 / QL_W, 24 / QL_H] },           // 第4の腕（左・鎖は座の上）
    { role: 'body', tex: 'torso', ox: 0, oy: -16, origin: [0.5, 0] },
    { role: 'dome', tex: 'crown', ox: 0, oy: -40, origin: [0.5, 0] },
    { role: 'rack', tex: 'lotus', ox: 0, oy: 1 },
    { role: 'armR', tex: 'armR', ox: 17, oy: -9, origin: [3 / ARMR_W, 24 / ARMR_H] },
    { role: 'armL', tex: 'armL', ox: -17, oy: -9, origin: [22 / ARML_W, 4 / ARML_H] },
    { role: 'core', tex: 'seed', ox: 0, oy: 1 },
  ],
  tier: { spriteScale: 4.2, glowScale: 11.0, glowOuter: '#2f8fd8', glowInner: '#ffedb0' },
};
validate(GAIKA);
